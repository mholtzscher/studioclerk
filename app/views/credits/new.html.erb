<h1>Add Credit</h1>

<%= render 'form', credit: @credit %>

<%= link_to 'Full Account History', student_credits_path, :class=>'btn btn-primary' %>
<%= link_to 'Back To Student', @student, :class=>'btn btn-primary' %>